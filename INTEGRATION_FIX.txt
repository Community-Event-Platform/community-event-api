Fix applied on branch: test-integration
Date: 2026-05-31

- Problem: API route `/registrations/{id}/approve` and `/registrations/{id}/reject` were mapped to `approveRegistration`/`rejectRegistration` but controller lacked these methods, causing "Call to undefined method" errors.
- Action: Added wrapper methods `approveRegistration()` and `rejectRegistration()` in `app/Http/Controllers/RegistrationController.php` delegating to existing `organizerApprove()` and `organizerReject()`.
- Verified: PHP lint OK; cleared route/config cache. Route list shows registration routes mapped to controller.

Commit instructions:
  cd community-event-api
  git checkout test-integration
  git add app/Http/Controllers/RegistrationController.php INTEGRATION_FIX.txt
  git commit -m "Fix: add approve/reject wrappers to RegistrationController (test-integration)"
